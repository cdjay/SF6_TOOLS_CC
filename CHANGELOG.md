# Changelog

## Unreleased

## 0.99 - 2026-07-22

### Major

- 完成 30 个角色的统一指令数据，使用 AC、BCM 与官方语义生成 `xt.command_display.v1` 三槽 ActionGraph。
- 新增可独立打包的训练动态配置管理组件，支持导入、导出、校验和自动备份，并修复导入导出时序不一致导致配置无法载入的问题。
- 连段训练支持 Raw Input 录制与版本化 JSON v2，可记录控制类型、角色资源和训练环境。

### Added

- 新增连段完成记录、失败自动重试及完成后自动切换下一条连段。
- 新增现代模式指令显示单选项，可选择简化、搓招或全部；搓招不存在时自动回退到简化指令。
- 新增现代模式加载经典连段选项：不改写连段文件，按统一表投影现代指令，并提前标记现代不可用步骤。
- 新增匿名连段训练事件记录，按规范化内容 SHA-256 区分连段版本，并分别记录人工训练与自动演示结果，供托盘后续上传。
- 角色指令表升级为统一 ActionGraph 输出，同一 Action ID 同时携带经典、现代简化和现代搓招指令投影。
- 新增全角色 BCM 运行时基础表、角色例外表与 AC/BCM/OFF 版本化生成和验证工具链。
- 新增线上对战运行时安全控制，避免训练功能影响非训练场景。
- 新增连段作者、上传日期、训练备注和分步骤备注显示。

### Changed

- 将连段验证器、动作匹配、角色规则、调试追踪和延迟吸收逻辑拆分为独立模块。
- 经典、现代简化和现代搓招显示统一读取三槽角色表；角色例外表不再覆盖指令文本，仅保留检测行为。
- 角色例外表移除旧显示字段、显示专用条目和无效默认值空壳，仅保留 437 条跨控制模式共用的检测行为规则。
- 补齐 30 个角色统一表中的经典投影：经典仍以 BCM `norm/sprt` 为独立全集，现代作为其子集；所有现代 Action 均具备经典指令，生成期与加载期缺口必须为零。
- 统一指令表加载完成严格审计和路由解析后，仅缓存动作 ID、三个指令槽与解析状态，避免大型 JSON 长期驻留造成周期性 GC 卡顿。
- 改进连段 JSON 刷新、安全文件名、快捷键布局与 D2D 配置保存节流。

### Fixed

- 修复本田延迟吸收与架势派生、古烈蓄力招式、春丽行云流水、金柏莉和杰米现代指令等角色专项问题。
- 修复重复普通技、多段命中、同动作续段、压力尾段、版边晕厥演示和防御状态恢复等连段判定问题。
- 修复训练菜单切换、脚本重载、网页同步及配置导入过程中可能出现的状态残留和刷新问题。

## 0.9a - 2026-06-29

### Fixed

- 共享热键绑定支持键盘与 HID/GamePad 设备按钮，手柄、Hitbox、摇杆等可被游戏识别的设备可自由绑定训练快捷键。
- 修复设备按键点按绑定无反应的问题，并增加游戏输入流兜底与热键菜单设备检测显示。
- 保留旧版键盘热键配置兼容，已保存的 `{ vk, mods }` 绑定仍可继续使用。

## 0.9 - 2026-06-28

### Major

- 将训练运行时的帧稳定性作为重点优化目标，降低开启 MOD 后的细微掉帧和体感卡顿。
- 移除连段训练中每 60 帧全量扫描并解析连段 JSON 的轮询逻辑，避免连段文件数量较多时产生周期性卡顿。

### Changed

- RuntimeSafety 训练上下文检查改为每帧缓存一次，减少多个模块重复扫描 TrainingManager 和原生 UI widget。
- 训练管理器的 inactive WebState 写入改为状态变化优先、低频刷新，避免非训练状态下每帧同步写文件。
- 原生训练 UI 可见性扫描改为状态变化或低频刷新，不再每帧遍历并写入 widget 可见性。
- Distance Viewer 的 AA 招式列表按对手角色和数据版本缓存，不再每帧重建排序。
- Shared input hook 热路径改为数字栈并避免重复 finalizer 注册。

## 0.8e - 2026-06-26

### Fixed

- 修复嘉米连段中瞬狱杀指令显示错误。
- 修复训练快捷键拆分后的注册与调用问题。
- 调整停止训练操作的文字说明为“返回”。

### Changed

- 将连段训练 JSON 文件处理逻辑拆分为独立模块。
- 将连段训练、确认训练、距离显示的快捷键逻辑拆分为独立模块。
- 使用共享快捷键注册与配置模块统一管理训练功能快捷键。

## 0.8c - 2026-06-25

### Fixed

- 修复连段验证中当前攻击已在动作帧计入 `combo_cnt` 时，被错误判定为上一动作连击数不匹配的问题。
- 修复豪鬼 `2LP -> 5LK` 连段在第 3 步触发 `WRONG MOVE`，导致玩家复现和自动演示无法继续的问题。
- 修复瞬狱杀只显示技能名称、未显示 `LP LP 6 LK HP` 正确指令的问题。
- 恢复连段失败现场写入 `LastFail.json`，记录动作 ID、步骤、实时连击数、期望连击数和 HP。
- 修复距离显示和碰撞框在角色离开训练场中心后按双倍位移漂移的问题；碰撞偏移已是世界坐标，不再重复叠加角色位置。

### Added

- 中文化界面与术语替换
- 汉化连段指令附加文本，包括“强化”“瞬狱杀”“奖牌”“等级”“完美”“延迟”等。
- 中文字体支持
- 中文 Guides
- 训练 JSON 元数据字段 `_xt_meta`
- 默认作者设置
- 保存训练时填写名称、备注、作者、标签
- 网页训练管理器
- 本地训练扫描
- 网络 Manifest 训练库
- 训练安装 / 删除 / 更新
- `sync_signal.json` 无感刷新机制
- 项目来源、原作者署名与二次开发说明文档
- 连段训练场景状态录制与恢复能力调研文档

### Changed

- Trial 统一翻译为“训练”
- Combo Trial 统一翻译为“连段训练”
- 页面项目名称统一为“小吞街霸6训练MOD”

### Notes

- 本项目基于 WTT / SF6_Tools 进行深度二次开发
- 保留原作者 Wael3rd 署名
- 原始 WTT 代码与设计归原作者所有
- 本项目新增部分由本项目维护者维护
